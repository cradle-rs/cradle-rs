Vendored Cilium CRD schemas (cilium.io/v2 CiliumEndpoint + CiliumNode),
copied verbatim from cilium/cilium v1.19.5
(`pkg/k8s/apis/cilium.io/client/crds/v2/`). cradle-k8s publishes these
objects when started with `--publish-crds`.
